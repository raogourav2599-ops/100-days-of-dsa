# Input
n = int(input())
arr = list(map(int, input().split()))

# Initialize
max_val = arr[0]
min_val = arr[0]

# Traverse array
for num in arr:
    if num > max_val:
        max_val = num
    if num < min_val:
        min_val = num

# Output
print("Max:", max_val)
print("Min:", min_val)
